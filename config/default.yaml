rasterizer:
  command: phantomjs
  port: 3001
  path: '/tmp/'
  viewport: '1024x600'
  cache:
    active: true
    path: '/tmp/cache/'
    cleanup: false
    cleanupInterval: 60000
    maxItems: 100
server:
  port: 3000
